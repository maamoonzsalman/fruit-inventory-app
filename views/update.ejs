<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/update.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <!-- Form for editing the fruit -->
        <h2 class="form-title">Edit Fruit</h2>
    
        <form action="/update/<%= fruit.name %>" method="POST">
          <!-- Fruit Name -->
          <label for="name">Fruit Name:</label>
          <input type="text" id="name" name="name" value="<%= fruit.name %>" required>
    
          <!-- Family -->
          <label for="family">Family:</label>
          <input type="text" id="family" name="family" value="<%= fruit.family %>" required>
    
          <!-- Order -->
          <label for="order">Order:</label>
          <input type="text" id="order" name="f_order" value="<%= fruit.f_order %>" required>
    
          <!-- Genus -->
          <label for="genus">Genus:</label>
          <input type="text" id="genus" name="genus" value="<%= fruit.genus %>" required>

          <input type="hidden" name="id" value="<%= fruit.id %>">
        
          <!-- Submit Button -->
          <button type="submit" class="submit-button">Update Fruit</button>
        </form>
      </div>

</body>
</html>